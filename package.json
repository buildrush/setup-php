{
  "name": "@buildrush/setup-php",
  "version": "0.0.0",
  "description": "Fast, reproducible PHP setup for GitHub Actions",
  "private": true,
  "main": "src/index.js",
  "scripts": {
    "lint": "eslint src/",
    "format": "prettier --write src/",
    "format:check": "prettier --check src/"
  },
  "devDependencies": {
    "eslint": "^8.0.0",
    "prettier": "^3.0.0"
  }
}
