name: manual
on:
  workflow_dispatch:
    inputs:
      force:
        description: 'Force rebuild even if digests match'
        type: boolean
        default: false

jobs:
  build:
    uses: ./.github/workflows/plan.yml
    with:
      force: ${{ inputs.force }}
